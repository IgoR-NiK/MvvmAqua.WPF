<UserControl x:Class="MainWPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MainWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBox Text="{Binding Title.Value, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="{Binding Title.Value}" />
        <TextBlock Text="{Binding Title.IsValid}" />

        <TextBox Text="{Binding Password.Value, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="{Binding Password.Value}" />
        <TextBlock Text="{Binding Password.IsValid}" />
        <Button Content="Новое окно" Command="{Binding Next}" />
        <Button Content="Вперёд" Command="{Binding Navigate}" />
        <Button Content="Закрыть" Command="{Binding Close}" />
    </StackPanel>
</UserControl>
